package Context.playerstates {

	import flash.display.DisplayObjectContainer;
	import Context.Player;

	public class NoneState implements IIPlayerState{
		public function update(p: Player, m:DisplayObjectContainer): void {
		}
	}
}